# Dereverb

Try to remove echo from poor audio recordings

## Setup

```bash
pip install --index-url https://download.pytorch.org/whl/cpu "torch==2.4.1" "torchaudio==2.4.1"
pip install "numpy>=2.0"
pip install "nemo_toolkit[asr]" soundfile "onnx==1.16.2" "ml-dtypes>=0.5.0"
```
